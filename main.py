from agent_config import HealthWellnessAgent
from context import UserSessionContext

# Initialize the agent and context (can be imported in Streamlit or other scripts)

agent = HealthWellnessAgent()
context = UserSessionContext(name="User", uid=1)
